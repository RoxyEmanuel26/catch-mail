name = "roxymail-email-worker"
main = "email-worker.js"
compatibility_date = "2024-03-27"

[vars]
# Production Backend Webhook URL
BACKEND_WEBHOOK_URL = "https://bimantara26-roxy-mail-backend.hf.space/api/webhook/inbound"

# Sensitive variables (WEBHOOK_SECRET and FALLBACK_EMAIL) are configured via Cloudflare Secrets:
# npx wrangler secret put WEBHOOK_SECRET
# npx wrangler secret put FALLBACK_EMAIL
